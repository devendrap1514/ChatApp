<%= button_to "Logout", logout_session_path, method: :delete %>

<!-- app/views/groups/index.html.erb -->
<h1>Groups</h1>
<ul>
  <% @groups.each do |group| %>
    <li><%= link_to group.name, group_path(group) %></li>
  <% end %>
</ul>
